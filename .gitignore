# Dependencies (for future Next.js migration)
node_modules/
.next/
out/

# Environment
.env
.env.local
.env.production

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp

# Vercel
.vercel/
.vercel

# Internal working docs (repo root is the deployed directory).
# Excluded by pattern rather than whole-directory, because git will not
# descend into an ignored directory and a negation inside one never applies.
scripts/*.md

# Reference recordings — large, and not ours to publish
assets/Screen Recording*.mov
assets/*.reference.mp4
